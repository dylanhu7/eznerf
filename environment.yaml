name: eznerf
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python
  - pytorch
  - torchvision
  - tqdm
  - configargparse
  - wandb
  - pytorch-cuda # remove this if you don't have an NVIDIA GPU